version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install --frozen-lockfile
    build:
      commands:
        - export NEXT_PUBLIC_TINA_CLIENT_ID=$TINA_CMS_CLIENTID
        - export TINA_TOKEN=$TINA_CMS_TOKEN
        - export GITHUB_BRANCH=$TINA_CMS_BRANCH
        - yarn run build
        - hugo
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
